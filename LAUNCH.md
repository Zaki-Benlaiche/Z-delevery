# دليل إطلاق Z-delivry 🚀 — التقنيات والبنية التحتية والنشر

دليل عملي كامل: ماذا نفعل في كل خطوة، **أين تدفع بالضبط**، والتقنيات وطريقة النشر (Déploiement).
الرموز: 👤 = مهمّتك (حساب/دفع/قرار) · 🤖 = مهمّة المطوّر (كود/إعداد) · 💳 = نقطة دفع.

---

## 0) التقنيات المستخدمة (Stack) — ماذا يعمل وأين

| المكوّن | التقنية | يُستضاف على | الحالة |
|--------|---------|--------------|--------|
| الخادم (Backend/API) | FastAPI (Python) + Docker | **Render** | حيّ (مجاني) |
| قاعدة البيانات | PostgreSQL 16 + PostGIS | **Render** | حيّة (مجانية، تنتهي بعد 90 يوماً) |
| مخزن OTP/الجلسات | Redis (أو ذاكرة مؤقّتة) | **Render** أو Upstash | ذاكرة مؤقّتة الآن |
| تطبيق الزبون/التاجر/السائق | React Native + Expo | **EAS** (بناء) | لم يُبنَ للإنتاج بعد |
| لوحة التاجر (ويب) | React + Vite | **Vercel** | حيّة |
| رفع الصور | Cloudinary | Cloudinary | مضبوط ✅ |
| الإشعارات | Expo Push | Expo | الكود جاهز |
| الخرائط | Google Maps SDK | Google Cloud | مفتاح placeholder ❌ |
| الرسائل (OTP) | — | مزوّد SMS | **غير مدمج** ❌ |
| الدفع | نقداً (cash) | — | يعمل ✅ / البطاقة غير مدمجة |

**الروابط الحالية:**
- API: https://zdelivry-api.onrender.com (و `/docs` و `/health`)
- ويب: https://z-delevery.vercel.app
- GitHub: https://github.com/Zaki-Benlaiche/Z-delevery (branch `main`)

---

## 1) أين تدفع — كل الحسابات والأسعار في مكان واحد 💳

| الخدمة | الرابط | الخطة المطلوبة | التكلفة | الدفع بـ |
|--------|--------|----------------|---------|----------|
| **Render — API** | dashboard.render.com | Starter (بلا نوم) | ~7$/شهر | بطاقة دولية |
| **Render — PostgreSQL** | dashboard.render.com | Basic (دائمة) | ~6-7$/شهر | بطاقة دولية |
| **Redis** | upstash.com | Free (10k أمر/يوم) | مجاني للبداية | — |
| **مزوّد SMS** | (حسب الاختيار أدناه) | حسب الاستهلاك | لكل رسالة | بطاقة/اتفاق محلّي |
| **Google Maps** | console.cloud.google.com | تفعيل الفوترة | مجاني فعلياً* | بطاقة (لا تُخصَم ضمن الحد) |
| **Cloudinary** | cloudinary.com | Free (25 وحدة/شهر) | مجاني | — مضبوط ✅ |
| **EAS Build** | expo.dev | Free أو محلّي | مجاني (أو 99$/شهر للأولوية) | اختياري |
| **Google Play** (اختياري) | play.google.com/console | حساب مطوّر | 25$ مرة واحدة | بطاقة |

\* خرائط Android (Maps SDK) ضمن رصيد Google الشهري المجاني (200$) — عملياً مجانية لتطبيق توصيل صغير، لكن تفعيل الفوترة (بطاقة) إجباري لإنشاء المفتاح.

> **الحدّ الأدنى للإطلاق:** Render API + PostgreSQL مدفوعان (~14$/شهر) + مزوّد SMS. الباقي مجاني/اختياري.

---

## 2) المراحل التفصيلية — ماذا تفعل وكيف ننشر

### المرحلة 1 — SMS/OTP 🔴 (العائق الأكبر، يجب أولاً)
**لماذا:** الآن `OTP_DEV_MODE=true` → الرمز يُرجَع في استجابة الـ API = أي شخص يدخل بأي رقم. ولا مزوّد إرسال.

**خيار أ — Twilio (دولي، الأسرع إعداداً):**
- 👤 سجّل في twilio.com → احصل على `Account SID` + `Auth Token` + رقم مُرسِل (أو Messaging Service SID).
- 💳 الدفع: بطاقة دولية، ~0.06-0.12$ لكل رسالة للجزائر. رصيد تجريبي ~15$.
- ⚠️ في الحساب التجريبي ترسل فقط لأرقام موثّقة — تحتاج ترقية مدفوعة للإرسال للجميع.

**خيار ب — بوّابة SMS جزائرية محلّية:**
- 👤 اتفاق تجاري مع مزوّد محلّي يوفّر HTTP SMS API + اعتماد `Sender ID`.
- 💳 أرخص للأرقام الجزائرية، لكن إعداد أطول.

- 🤖 أدمج المزوّد في `backend/app/services/otp.py` خلف متغيّرات بيئة + أضيف حدّ معدّل (rate limit).
- 👤 على Render: أضِف مفاتيح المزوّد، وغيّر `OTP_DEV_MODE=false`.
- 🧪 اختبار: استلام رمز حقيقي والدخول.

### المرحلة 2 — ترقية البنية التحتية 🔴
**لماذا:** المجانية تنام (أول طلب ~50ث)، وقاعدة البيانات تُحذف بعد 90 يوماً، وOTP في الذاكرة يُمسح عند كل إعادة تشغيل.

- 👤💳 dashboard.render.com → خدمة `zdelivry-api` → Settings → Plan → **Starter (~7$/شهر)**.
- 👤💳 أنشئ **PostgreSQL مدفوعة دائمة** (الخطة المجانية الحالية تنتهي) → انسخ `Internal Database URL`.
- 👤 أنشئ Redis مجاني على upstash.com → انسخ `redis://...` (rediss TLS).
- 🤖 حدّث `render.yaml`: `REDIS_URL` الحقيقي، وربط قاعدة البيانات الجديدة، وتضييق `ALLOW_ORIGINS` لنطاق الويب.
- 🤖 رحّل بيانات قاعدة البيانات للجديدة (إن وُجدت بيانات تهمّك).

### المرحلة 3 — الدفع 🟠
- 👤 قرار: **نقداً فقط** للإطلاق (موصى به).
- 🤖 إن نعم: إخفاء خيار «البطاقة» من السلّة (تعديل واجهة بسيط).
- 👤 (لاحقاً) اتفاق مع **SATIM** (CIB/Edahabia) أو **Chargily** للدفع الإلكتروني.
- 🤖 (لاحقاً) دمج البوّابة والتحقّق من الدفع.

### المرحلة 4 — مفتاح الخرائط 🟠
- 👤💳 console.cloud.google.com → مشروع جديد → فعّل **Maps SDK for Android** → فعّل الفوترة (بطاقة) → أنشئ **API Key** → قيّده باسم الحزمة `com.zdelivry.app` + بصمة SHA-1.
- 🤖 ضع المفتاح في `app.json` → تعود الخريطة تلقائياً (علم `MAPS_ENABLED` جاهز).
- 🤖 إعادة بناء APK (تغيير native، ليس OTA).

### المرحلة 5 — المحتوى والتشغيل 🟠
- 👤 ضبط `ADMIN_PHONES` على Render (رقمك) للدخول للوحة الأدمن.
- 👤 إدخال متاجر/منتجات/صور/عروض حقيقية (أو تجنيد التجّار ليسجّلوا).
- 👤 تجنيد سائقين + توثيقهم (`is_verified`) من لوحة الأدمن.
- 👤 مراجعة التسعير: أساسي 200 دج + 30 دج/كم + عمولة 10% (قابلة للتعديل عبر متغيّرات البيئة).

### المرحلة 6 — البناء والتوزيع 🟠
- 👤 شعار وأيقونة نهائيان.
- 🤖 ضبط `app.json` (الاسم/الأيقونة/الإصدار/`runtimeVersion`).
- 🤖 `eas build -p android --profile production` → ينتج APK/AAB.
- 👤 التوزيع:
  - **APK مباشر** (مجاني، الأسرع): توزّع ملف `.apk` على التجّار/الزبائن (يفعّلون «مصادر غير معروفة»).
  - **Google Play** (25$ مرّة واحدة): تجربة أرسمَ + مراجعة Google (أيام).

### المرحلة 7 — قانوني واختبار 🟠
- 🤖 صياغة مسودّة شروط استخدام + سياسة خصوصية.
- 👤 اعتمادها.
- 🧪 اختبار الدورة الكاملة بأطراف حقيقية + عدّة أجهزة.

### المرحلة 8 — الإطلاق 🟢
- إطلاق تجريبي مغلق (حيّ/مدينة واحدة) → متابعة الأخطاء → إطلاق عام.

---

## 3) كيف ننشر (Déploiement) — العملية خطوة بخطوة

### أ) الخادم (Backend) — Render
- النشر **تلقائي**: كل `git push origin main` يُطلق بناءً جديداً على Render (Docker من `backend/Dockerfile`).
- المتغيّرات الحسّاسة تُضبط من لوحة Render (Environment) لا في git.
- التحقّق: `https://zdelivry-api.onrender.com/health` يرجع `ok`.

### ب) الموبايل — Expo / EAS
- **بناء أوّلي (APK):** `eas build -p android --profile production` على expo.dev (حساب `benlaiche_zakaria`).
- **تحديثات لاحقة:**
  - تعديلات JS فقط → **OTA** فوري بـ `eas update` (دون متجر) — بشرط ثبات `runtimeVersion`.
  - تغييرات native (مكتبات/أذونات/مفتاح خرائط) → **بناء APK جديد** إجباري.

### ج) الويب (لوحة التاجر) — Vercel
- النشر **تلقائي** من `git push` (مجلّد `web/`), مع `VITE_API_URL` يشير للـ API.

---

## 4) جدول التكاليف الشهرية (MVP)
| البند | التكلفة |
|------|---------|
| Render API (Starter) | ~7$/شهر |
| PostgreSQL مدفوعة | ~6-7$/شهر |
| Redis (Upstash free) | مجاني |
| SMS | حسب الاستهلاك (لكل رسالة) |
| Maps + Cloudinary | مجاني فعلياً |
| Google Play (اختياري) | 25$ مرّة واحدة |
| **الإجمالي الثابت** | **~14$/شهر + رسوم SMS** |

---

## ✅ أولوياتك الآن (بالترتيب)
1. 👤 **مزوّد SMS** (Twilio أم محلّي) — يفكّ العائق الأكبر.
2. 👤💳 **ترقية Render** (API + PostgreSQL دائمة).
3. 👤 **قرار الدفع** (نقداً فقط للبداية؟).
4. 👤💳 **مفتاح Google Maps** (بعد تفعيل 2SV).

> فور تجهيز أي بند من 👤، يبدأ الجزء البرمجي (🤖) فوراً.
